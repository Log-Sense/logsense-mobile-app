import { Switch } from "@/gluestack-ui-components/ui/switch";
import { CardContent, CardIcon, CardRoot, CardTitle } from "./card";

export const Card = {
  Root: CardRoot,
  Content: CardContent,
  Title: CardTitle,
  Icon: CardIcon,
  Switch: Switch,
}